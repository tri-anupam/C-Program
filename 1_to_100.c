#include <stdio.h>
#include<string.h>
// -----This program is goes to print 1 to 100 number without using any number in program-----------
int main()
{
    int one='a'/'a';
    char s[]="..........";
    int last=strlen(s);
    for(int i=one;i<=(last*last);i++)
    {
        printf("%d\n",i);
    }
    return 0;
}